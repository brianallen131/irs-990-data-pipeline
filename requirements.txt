# Python version
python>=3.12,<3.13

# Core data processing
pandas>=2.1.0

# XML parsing (built-in, but ensuring compatibility)
# lxml is optional but recommended for better XML performance
lxml>=5.0.0

# ZIP file handling with deflate64 support
zipfile-deflate64>=0.2.0

# Web scraping and HTTP requests
requests>=2.31.0
beautifulsoup4>=4.12.0

# Progress bars
tqdm>=4.66.0

# Testing (optional but recommended)
pytest>=7.4.0
pytest-cov>=4.1.0